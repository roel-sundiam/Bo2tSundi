{
  "version": 2,
  "builds": [
    {
      "src": "analytics-dashboard/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/analytics-dashboard" }
    },
    {
      "src": "api/[^_]*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
